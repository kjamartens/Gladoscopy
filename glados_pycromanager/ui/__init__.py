"""Qt UI helpers (widget builders, markdown viewer, …).

Carved out of `GUI/utils.py` in Phase 7. Most call sites continue to
import from `utils` via thin re-export shims; new code should reach
straight into this subpackage.
"""
